import { NgxMaterialTimepickerDirective } from './ngx-material-timepicker.directive';

describe('NgxMaterialTimepickerDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxMaterialTimepickerDirective();
    expect(directive).toBeTruthy();
  });
});
